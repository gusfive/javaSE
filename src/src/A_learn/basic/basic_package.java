package A_learn.basic;

public class basic_package {
    /**API，全称Application Programming Interface,即应用程序编程接口
     *包是用来分门别类的管理各种不同程序的，类似文件夹，建包有利于程序的管理和维护。
     * 建包的语法格式：package com.wqr.javalearn;
     * 在自己程序中调用其他包下的程序的注意事项：
     * 1.同一个包下的程序，可以直接访问；
     * 2.访问其他包下的程序，必须导包才可以访问；import com.wqr.javalearn.Demo1;
     * 3.自己的程序中调用JAVA提供的程序，也需要先导包才可以使用；注意，Java.lang包下的程序不需要我们导包，可以直接使用；
     * 4.访问多个其他包下的程序，这些程序名又一样的情况下，默认只能导入一个程序，另一个程序必须带包名和类名来访问。
     */
}
